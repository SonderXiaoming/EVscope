# Per-figure reproducibility commands

Add one subdirectory per manuscript figure. Each subdirectory should include:

- `README.md`: exact command(s), input paths/accessions, output files
- `inputs.tsv`: input artifact IDs matching `repro/manifest.tsv`
- `expected_outputs.tsv`: output filenames, formats, checksums if stable
